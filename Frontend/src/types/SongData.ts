export type SongData = {
  /**
   * Which deck is the info for
   */
  deckNum: number
  /**
   * Current track's title
   */
  trackTitle: string
  /**
   * Current track's artist
   */
  artistName: string
}
